# ====================================================
# requirements.txt
# 이 파일은 프로젝트에 필요한 파이썬 패키지 목록이야.
# 설치 명령어: pip install -r requirements.txt
# ====================================================

# ── 한국어 처리 ──────────────────────────────────────
jamo==0.4.1                  # 한글 자모 분리/합성 (예: 가 → ㄱㅏ)
g2pk==0.9.4                  # 한국어 발음 변환 (예: 먹어 → 머거)
konlpy==0.6.0                # 한국어 형태소 분석기

# ── AI 모델 (소형) ───────────────────────────────────
transformers==4.40.0         # Hugging Face 모델 허브 (BERT 계열)
torch==2.11.0                # 딥러닝 프레임워크 (모델 학습용)
datasets==2.19.0             # 데이터셋 로딩 도구

# ── AI 모델 (대형 LLM API) ───────────────────────────
openai==1.25.0               # GPT-4o API 호출용
# HyperCLOVA X는 별도 CLOVA Studio SDK 사용 (configs/llm.yaml 참고)

# ── 데이터 처리 및 분석 ──────────────────────────────
pandas==3.0.2              # 표 형태 데이터 처리
numpy==1.26.4                # 수치 계산
scikit-learn==1.4.2          # F1 score 등 성능 지표 계산

# ── 결과 시각화 ──────────────────────────────────────
matplotlib==3.8.4            # 그래프 그리기
seaborn==0.13.2              # 예쁜 그래프 (히트맵 등)

# ── 실험 관리 ────────────────────────────────────────
pyyaml==6.0.1                # YAML 설정 파일 읽기
tqdm==4.66.2                 # 진행 바 표시 (학습 중 얼마나 됐는지)

# ── Jupyter Notebook (선택 사항) ─────────────────────
jupyter==1.0.0               # 노트북 환경 (결과 확인용)
ipykernel==6.29.4

datasets==2.19.0