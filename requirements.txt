# ── 한국어 처리 ──────────────────────────────────────
jamo==0.4.1                  # 한글 자모 분리/합성
g2pk==0.9.4                  # 한국어 발음 변환
konlpy==0.6.0                # 한국어 형태소 분석기

# ── AI 모델 (소형) ───────────────────────────────────
transformers==4.40.0         # Hugging Face 모델 허브
torch>=2.2.0                 # 딥러닝 프레임워크 (2.11.0은 찾을 수 없는 버전일 수 있어, 안정적인 2.x 최신 버전으로 자동 설치되게 수정)
datasets==2.19.0             # 데이터셋 로딩 도구

# ── AI 모델 (대형 LLM API) ───────────────────────────
openai==1.25.0               # GPT-4o API 호출용
# HyperCLOVA X는 별도 CLOVA Studio SDK 사용

# ── 데이터 처리 및 분석 ──────────────────────────────
# [핵심 수정] numpy 1.26.4와 호환되도록 pandas를 2.x 버전으로 낮춤
pandas==2.2.2                
numpy==1.26.4                
scikit-learn==1.4.2          

# ── 결과 시각화 ──────────────────────────────────────
matplotlib==3.8.4            
seaborn==0.13.2              

# ── 실험 관리 ────────────────────────────────────────
pyyaml==6.0.1                
tqdm==4.66.2                 

# ── Jupyter Notebook (선택 사항) ─────────────────────
jupyter==1.0.0               
ipykernel==6.29.4