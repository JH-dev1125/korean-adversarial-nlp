# ====================================================
# requirements.txt
# Kaggle/Colab에서 현재 소형 모델 실험을 재현하기 위한 필수 패키지
#
# 원칙:
# - 현재 코드에서 실제 import되는 패키지만 기본 설치 대상으로 둔다.
# - Kaggle에 이미 설치된 패키지와 충돌을 줄이기 위해 데이터/시각화 패키지는 범위로 제한한다.
# - Hugging Face Trainer가 peft를 import할 때 생기는 버전 충돌을 피하기 위해
#   transformers/peft/accelerate는 서로 맞는 버전으로 고정한다.
# ====================================================

# ── AI 모델 학습/평가 ───────────────────────────────
transformers==4.44.2
peft==0.13.2
accelerate==0.34.2
datasets>=2.19.0,<3.0.0
torch>=2.2.0,<3.0.0

# ── 데이터 처리 및 평가 ─────────────────────────────
pandas>=2.2.0,<3.0.0
numpy>=1.26.0,<3.0.0
scikit-learn>=1.4.0,<2.0.0
pyyaml>=6.0.1,<7.0.0

# ── 결과 시각화 ─────────────────────────────────────
matplotlib>=3.8.0,<4.0.0
seaborn>=0.13.0,<0.14.0
tqdm>=4.66.3,<5.0.0

# ── 현재 코드에서는 사용하지 않는 선택 패키지 ─────
# LLM 평가 코드를 추가할 때: openai
# jamo/g2pk/konlpy는 과거 계획에 있었지만 현재 공격 코드는 자체 한글 유틸을 사용한다.
